export const numbersPanel = [7, 8, 9, 4, 5, 6, 1, 2, 3, 0, `.`];
export const controlSigns = [`AC`, `+/-`, `%`];

export enum actions {
  '/' = `division`,
  '*' = `multiplication`,
  '-' = `subtraction`,
  '+' = `addition`,
  '=' = `equal`
}